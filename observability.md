# Observability

Logs:
Docker container logs using:
docker logs <container_id>

Metrics:
Monitor CPU and memory via CloudWatch.

Alerts:
High CPU alert if CPU > 80%
Instance status check failure alert
